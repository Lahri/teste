# Copie para .env e ajuste. Em dev local com docker-compose estes valores
# ja batem com o container Oracle definido no docker-compose.yml -- so
# precisa trocar quando apontar pro Oracle real do SESC.
ORACLE_DSN=localhost:1521/FREEPDB1
ORACLE_USER=painel_demandas
ORACLE_PASSWORD=dev_local_only
CORS_ORIGINS=*
